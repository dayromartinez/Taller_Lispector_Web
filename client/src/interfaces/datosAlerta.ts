export interface datosAlerta {
    title?: string, 
    description?: string, 
    date?: string, 
    time?: string, 
    address?: string,
    imagen?: string,
    autor?: string,
    ilustrador?: string
}