export type commentData = {
    _id?: string,
    userId?: string,
    nombreUsuario?: string,
    publicacionId?: string,
    contenidoId?: string,
    comentarioId?: string,
    comentario?: string,
    valoracion?: number,
    updatedAt?: string,
}