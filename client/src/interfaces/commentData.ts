export type commentData = {
    userId?: string,
    nombreUsuario?: string,
    publicacionId?: string,
    comentarioId?: string,
    comentario?: string,
    valoracion?: Number,
    updatedAt?: string,
}